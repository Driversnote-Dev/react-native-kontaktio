'use strict';

import { NativeModules } from 'react-native';

// If the native module (i.e. Java module) is prefixed with "RCT", 
// the NativeModules name does not include "RCT". 

export default NativeModules.Kontakt;